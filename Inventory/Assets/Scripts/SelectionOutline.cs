using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.EventSystems;
using DG.Tweening;

public class SelectionOutline : MonoBehaviour
{
    //[SerializeField] private SlotHover slotHover;

    private void Update()
    {
        //slotHover.OnHoverOverSlot.AddListener(MoveSelection);
    }

    private void MoveSelection()
    {
        //Debug.Log("Hovered over slot fix?");
        //transform.DOMove()
    }
}
